{
  "name": "azeroth-campaign-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first, multi-character World of Warcraft completion and gold campaign dashboard.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "check": "npm run build && node scripts/check.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
